pipeline {
    agent any

    stages {
        stage('Remove psycopg2 and psycopg2-binary in requeriments.txt') {
            steps {
                sh 'pwd'
            }
            steps {
                sh 'ls'
            }
        }
        stage('Add psycopg2 file at project') {
            steps {
                sh 'pwd'
            }
            steps {
                sh 'ls'
            }
        }
    }
}
